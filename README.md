# For a React + Vite + Electron + Java Springboot Backend

# https://github.com/nikcich/NixplorerBackend

### For future reference to myself,
### "build": "vite build --base=./" fixes the problem serving the index.html build file.